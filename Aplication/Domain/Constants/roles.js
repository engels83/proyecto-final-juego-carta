const admin = { name: "Admin", description: "Manage users and roles" };
const player = { name: "Player", description: "play games and share matches" };
const superAdmin = { name: "Superadmin", description: "the Boss!" };

module.exports = [].concat(admin, player, superAdmin);
